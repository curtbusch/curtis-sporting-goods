<h1>Categories</h1>
<ul class="list-group">
  <% @categories.each do |category| %>
    <li class="list-group-item"><%= link_to category.category_name, products_path(category_index: category.id) %></li>
  <% end %>
</ul>